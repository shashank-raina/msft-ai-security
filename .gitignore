# ── Wrangler / Cloudflare ─────────────────────
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# ── Private — never commit these ──────────────
config.js
update-agent.html
sources.json

# ── OS / editor junk ──────────────────────────
.DS_Store
Thumbs.db
.vscode/
*.swp
